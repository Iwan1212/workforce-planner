services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: workforce_planner
      POSTGRES_USER: workforce
      POSTGRES_PASSWORD: workforce_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
